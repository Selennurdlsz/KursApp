import { DersFiltrePipe } from './ders-filtre.pipe';

describe('DersFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new DersFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
